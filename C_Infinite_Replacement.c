#include<stdio.h>
#include<string.h>
int main()
{
    int n;
    char s[50],t[50];
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%s %s",&s,&t);
        int size1=strlen(s);
        int size2=strlen(t);
        
    }
}